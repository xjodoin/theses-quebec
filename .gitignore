# secrets
.env
*.env.local

# python
.venv/
__pycache__/
*.pyc
*.pyo

# project scratch
data/llm_jobs/
data/llm_preview.jsonl
# The DB is now distributed via GitHub Releases (scripts/release_db.mjs
# / scripts/fetch_db.mjs). Contributors run `npm run db:fetch` after
# cloning instead of pulling it via LFS.
data/theses.db
data/theses.db-journal
data/theses.db-wal
data/theses.db-shm

# Node (build pipeline)
node_modules/
dist/

# Java benchmark build output
target/

# tooling caches
.playwright-mcp/
.claude/
/tmp/
